#
# Overriden variables for the Vagrant VM
#

neo4j_package: neo4j-enterprise
neo4j_package_version: 2.0.0

# The port here must be the same as in Vagrantfile
neo4j_server_webserver_port: 7474

# Allow access from anywhere to access from host
neo4j_server_webserver_address: 0.0.0.0