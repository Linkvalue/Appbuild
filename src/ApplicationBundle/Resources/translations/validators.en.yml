# Appbuild => ApplicationBundle => validators.en
application:
    form:
        must_set_package_name: This application OS requires to set a Package name.
build:
    form:
        must_upload_file: You have to upload the build file.
